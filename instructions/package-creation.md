# Invariant Package Creation — package structure, manifest format, item types and installed naming

When you need to create an Invariant package, use this instruction as your reference. To scaffold a package interactively with prompts and file generation, invoke the [`invariant.guru-package-create-package`](.claude/skills/invariant.guru-package-create-package/SKILL.md) skill.

## This Package Contains

| Item | Type | Installed as | When to use |
|------|------|--------------|-------------|
| `package-creation` | Instruction | folded into this context file | You're reading it. Reference for creating packages. |
| `create-package` | Skill | [`invariant.guru-package-create-package`](.claude/skills/invariant.guru-package-create-package/SKILL.md) | Invoke to interactively scaffold a new package with prompts and file generation. |
| `package-creator` | Agent | [`invariant.guru-package-package-creator`](.claude/agents/invariant.guru-package-package-creator.md) | Activate for a dedicated session focused entirely on designing and building a package. |

> **Partial installs:** Invariant lets users install only a subset of a package's items. Some items listed above may not be present in your `.claude` folder -- that's expected. Use what's available and refer to the descriptions above to understand what each item does if you need it.

## Package Structure

A package is a directory with a manifest, a README, and item directories:

```
my-package/
├── invariant.json           # manifest (required)
├── README.md                # human-facing description (required)
├── agents/                  # AI behavior specs
├── skills/                  # repeatable procedures (always folders)
├── commands/                # slash-command definitions
├── rules/                   # constraints and standards
├── contexts/                # background knowledge
└── instructions/            # procedural guides (folded into each target context file)
```

Only include directories for item types the package provides.

## Manifest

```json
{
  "name": "my-package",
  "version": "1.0.0",
  "description": "Short summary shown in search results.",
  "author": "my-handle",
  "license": "MIT",
  "repository": "https://github.com/my-handle/my-package",
  "items": {
    "rules": [{ "name": "naming", "description": "Enforces naming conventions." }],
    "skills": [{ "name": "setup", "description": "Scaffolds the project." }],
    "instructions": [{ "name": "guide", "description": "How to use this package." }]
  }
}
```

- `name`: lowercase, hyphens allowed, scoped (`@author/name`) if needed.
- `version`: semver (`major.minor.patch`).
- `items`: maps type directories to `{ name, description }` arrays. **Generated by `invariant package create` / `invariant package refresh`** — never hand-written. Each entry must match a file or folder on disk.

## Installed Names and Paths

Items are **renamed at install time**: the CLI prefixes each one with a slug of the
package name, so two packages shipping a `frontend` skill cannot overwrite each other.

```
slug(pkg)    = drop "@", "/"->"-", lowercase, non-[a-z0-9._-]->"-", collapse "-"
qualified()  = item, if item === slug or item starts with slug + "-"
               otherwise slug + "-" + item        # idempotent
```

`docs` + `frontend` → `docs-frontend`. `@acme/skills` + `frontend` →
`acme-skills-frontend`.

| Location | Prefixed | Path |
|---|---|---|
| Canonical store | yes | `.agents/<type>/<qualified>` (or `.md` for single files) |
| Claude Code | yes | `.claude/<type>/<qualified>` — symlink into `.agents/` |
| Cursor | yes | `.cursor/rules/<qualified>.mdc` |
| Copilot | yes | `.github/instructions/<qualified>.instructions.md` |
| Contexts, instructions | **no** | `.invariant/{contexts,instructions}/<package>/<name>.md` — already scoped by package, and folded into each target's context file |
| Your repository | **no** | keep authoring `skills/frontend/` — the prefix is an install-time projection |

Nothing rewrites your content, so **every link to one of your own items must spell the
prefixed path**. `.agents/` and the target folders are generated and gitignored — rebuilt
by `invariant sync`, never authored into, never committed. `invariant inspect <package>
--paths` prints the exact installed names; `invariant package validate` warns
(`unprefixed_path_reference`) about links that will 404.

## Item Types at a Glance

| Type           | Format                     | Purpose                                           |
|----------------|----------------------------|---------------------------------------------------|
| `agents/`      | `.md` file or folder       | Shape AI behavior for a class of tasks            |
| `skills/`      | folder with `SKILL.md`     | Repeatable on-demand procedures                   |
| `commands/`    | `.md` file or folder       | Custom `/slash` commands                          |
| `rules/`       | `.md` file or folder       | Constraints the AI must always follow             |
| `contexts/`    | `.md` file or folder       | Background knowledge for the AI                   |
| `instructions/`| `.md` file or folder       | Step-by-step guides embedded in AI context        |

All types except skills can be a single `.md` file or a folder of `.md` files. Skills must always be a folder containing at least `SKILL.md`.

## Writing Each Item Type

Agents get `## Role` / `## Approach` / `## Workflow`; skills get `## Usage` (when to
invoke) + `## Procedure` (ordered `### Step N`); commands get `## Trigger` /
`## Parameters` / `## Behavior`; rules and contexts are bullet lists. Instructions have
their own section below -- they require special attention.

```markdown
# Agent: Reviewer                 # Skill: Setup
## Role                           ## Usage
Expert in layer dependencies.     Invoke to scaffold the project.
## Workflow                       ## Procedure
1. Read the diff.                 ### Step 1 -- Gather inputs
2. Flag violations.               Ask for project name and framework.
3. Suggest fixes.                 ### Step 2 -- Generate files

# Command: scaffold               # Rule: Naming
## Trigger                        - camelCase for variables and functions.
/scaffold <name>                  - PascalCase for classes and types.
## Parameters                     - UPPER_SNAKE_CASE for constants.
- name: Component name.
## Behavior                       # Context: Tech Stack
1. Create folder.                 - Frontend: React + TypeScript
2. Generate template files.       - Backend: Node.js + Express
```

## Crafting Good Instructions

Instructions are the most important item type to get right. Unlike skills (invoked on demand) or agents (activated explicitly), **instructions are folded into every configured target's context file** — `CLAUDE.md` (claude), `AGENTS.md` (codex), `.cursor/rules/invariant-context.mdc` (cursor), `.github/copilot-instructions.md` (copilot), `CONVENTIONS.md` (aider) — and read on every interaction. Every token is paid for repeatedly.

### Principles

1. **Start with a package contents table.** Every instruction must open with a TLDR table listing all items in the package (name, type, when to use). This tells the AI what resources exist at a glance. Include a note that Invariant allows partial installs -- some items may not be in the `.claude` folder, and that's expected. Example:

   ```markdown
   ## This Package Contains
   | Item | Type | Installed as | When to use |
   |------|------|--------------|-------------|
   | `my-guide` | Instruction | folded into this context file | Reference, always in context. |
   | `my-skill` | Skill | `my-pkg-my-skill` | Invoke to run the interactive workflow. |
   | `my-agent` | Agent | `my-pkg-my-agent` | Activate for a dedicated session. |

   > **Partial installs:** Some items above may not be present in your
   > `.claude` folder. Invariant lets users install only a subset of a
   > package. Use what's available.
   ```

2. **Self-contained but concise.** The instruction must give the AI enough to act without invoking anything else. Include small inline examples, key rules, and the essential "how". But keep it tight -- avoid walls of text.

3. **Point to skills for depth.** When a topic has an interactive, multi-step workflow, summarize the key points inline and then explicitly direct the AI to invoke the skill. Example:

   > To scaffold a new package interactively, invoke the
   > [`my-pkg-setup`](.claude/skills/my-pkg-setup/SKILL.md) skill.

   This way, the AI can handle simple cases from the instruction alone, and delegate complex cases to the skill.

4. **Spell installed paths.** A reference to your own item must carry the package
   prefix, because the instruction is read from a context file where no rewriting
   happens:

   ```markdown
   <!-- package my-pkg -->
   YES: [my-pkg-setup](.claude/skills/my-pkg-setup/SKILL.md)
   NO:  [setup](.claude/skills/setup/SKILL.md)          <- 404 after install
   NO:  [setup](.agents/skills/setup/SKILL.md)          <- same, and .agents/ is generated
   ```

5. **Lead with action, not theory.** Start with what to do, not background. The AI doesn't need a history lesson -- it needs a clear procedure.

6. **Include examples for every concept.** A 3-line example is worth a paragraph of explanation. Use fenced code blocks with realistic content.

7. **Use checklists for validation.** Checkboxes make it easy for the AI to verify its work systematically.

### Instruction Template

```markdown
<!-- package name: <package>  ->  slug: <slug> -->
# <Topic>

<One sentence: what this instruction covers and when to use it.>
<One sentence: reference the relevant skill for interactive/deep workflows.>

## This Package Contains

| Item | Type | Installed as | When to use |
|------|------|--------------|-------------|
| `<instruction>` | Instruction | folded into this context file | <short description> |
| `<skill>` | Skill | `<slug>-<skill>` | <short description> |

> **Partial installs:** Some items above may not be present in your
> `.claude` folder. Invariant lets users install only a subset of a
> package. Use what's available.

## <Core Section>

<Concise explanation with inline example.>

## <Another Section>

<Concise explanation with inline example.>

## Checklist

- [ ] First validation point.
- [ ] Second validation point.

For interactive guidance, invoke the
[<slug>-<skill>](.claude/skills/<slug>-<skill>/SKILL.md) skill.
```

Substitute the computed slug — never ship `<slug>` as a literal.

### Anti-patterns to Avoid

- **No package contents table.** Without it, the AI doesn't know what's available in the package and can't discover skills or agents on its own.
- **No skill reference.** If the package has a skill related to the instruction, the instruction MUST reference it. Otherwise the AI won't know the skill exists.
- **No partial-install note.** Users can install subsets of a package. If the instruction doesn't mention this, the AI may error when an item is missing.
- **Too verbose.** Instructions are loaded on every interaction. A 500-line instruction wastes tokens. Aim for the minimum needed to act.
- **Too sparse.** If the instruction just says "use the skill", the AI can't handle simple cases without invoking it. Include enough inline detail.
- **No examples.** Abstract rules without examples lead to inconsistent AI behavior. Always show, don't just tell.
- **Unprefixed self-reference.** `.claude/skills/setup/SKILL.md` for your own `setup` skill is a dead link the moment the package is installed under any name — the file is at `.claude/skills/<slug>-setup/SKILL.md`. `invariant package validate` warns about this.
- **Treating `.claude/` as the store.** `.claude/<type>/<name>` is a symlink view of the canonical `.agents/<type>/<name>`, and both are generated by `invariant sync`. Never tell the AI to edit or commit them.

## README.md

Every package must include a `README.md`:

```markdown
# my-package
Short description.

## Items
| Type        | Name   | Description                    |
|-------------|--------|--------------------------------|
| Rule        | naming | Enforces naming conventions.   |
| Skill       | setup  | Scaffolds the project.         |
| Instruction | guide  | How to use this package.       |

## Install
invariant install @author/my-package

## Author
my-handle

## License
MIT
```

## Validation Checklist

- [ ] `README.md` exists at the package root.
- [ ] `invariant.json` has `name`, `version`, `description`, `author`, `license`.
- [ ] Every item in the manifest has a matching file or folder on disk.
- [ ] Skills are folders containing `SKILL.md`.
- [ ] Instructions open with a "This Package Contains" table listing all items.
- [ ] Instructions include a partial-install note.
- [ ] Instructions reference related skills when they exist.
- [ ] Instructions are concise with inline examples.
- [ ] Package name is lowercase, hyphens only (scope allowed: `@author/name`).
- [ ] Version follows semver.
- [ ] Installed names computed from the package slug before content was authored.
- [ ] Every reference to one of the package's own items spells the prefixed path.
- [ ] `invariant package validate` reports no drift and no `unprefixed_path_reference` warnings.
- [ ] `.agents/` and target folders were never hand-edited or committed — they are generated.

To scaffold a complete package interactively, invoke the [`invariant.guru-package-create-package`](.claude/skills/invariant.guru-package-create-package/SKILL.md) skill.
